# Inventory System Architecture

## Module Dependency Graph

```
┌─────────────────────────────────────────────────────────────┐
│                     src/inventory/mod.rs                    │
│                    (Public API Exports)                     │
└──────────────┬──────────────┬──────────────┬────────────────┘
               │              │              │
       ┌───────▼────┐  ┌──────▼──────┐  ┌───▼────────┐
       │  item.rs   │  │ inventory.rs│  │ crafting.rs│
       │            │  │             │  │            │
       │  • Item    │  │ • Inventory │  │ • Grid     │
       │  • Stack   │  │ • 36 slots  │  │ • Recipes  │
       └────┬───────┘  └──────┬──────┘  └─────┬──────┘
            │                 │                │
            │        ┌────────▼────────┐       │
            └────────►   tools.rs      ◄───────┘
                     │                 │
                     │ • Tool          │
                     │ • ToolTier      │
                     │ • ToolType      │
                     └─────────────────┘
                             │
                             ▼
                     ┌───────────────┐
                     │ types.rs      │
                     │ (BlockType)   │
                     └───────────────┘
```

## Data Flow

### Crafting Workflow

```
Player Actions              System State               World State
─────────────              ────────────               ───────────

1. Break Oak Log
       │
       ├────────────────► Add Item::Block(OakLog)
       │                  to Inventory
       │                         │
2. Open Crafting UI              │
       │                         │
3. Place log in grid             │
       │                         │
       ├────────────────► Remove 1 OakLog from Inventory
       │                         │
       │                  Set CraftingGrid slot[0]
       │                         │
       │                  update_output() matches recipe
       │                         │
       │                  Output: 4× OakPlanks
       │                         │
4. Click output                  │
       │                         │
       ├────────────────► take_output()
       │                         │
       │                  Consume 1 OakLog from grid
       │                         │
       │                  Add 4× OakPlanks to Inventory
       │                         │
5. Place 2 planks in grid        │
       │                         │
       ├────────────────► Remove 2 OakPlanks
       │                         │
       │                  Match stick recipe
       │                         │
       │                  Output: 4× Sticks
       │                         │
6. Take sticks                   │
       │                         │
       └────────────────► Add 4× Sticks to Inventory
```

### Mining Workflow with Tools

```
Player Actions              Inventory State            World State
─────────────              ───────────────            ───────────

Selected: Wooden Pickaxe
Durability: 60/60
       │
1. Start mining stone block
       │
       ├────────────────► Calculate break_time:
       │                  Tool::break_time(Stone)
       │                  = 1.5 / 2.0 = 0.75s
       │                         │
2. Wait 0.75 seconds             │
       │                         │
3. Block breaks                  │
       │                         │
       ├────────────────► damage_tool(1)         ──► Remove Stone block
       │                  Durability: 59/60           │
       │                         │                    │
       ├────────────────► can_harvest(Stone)?   ◄────┘
       │                  = true                      │
       │                         │                    │
       ├────────────────► Add Cobblestone         ◄───┘
       │                  to Inventory
       │
... (repeat 59 times)
       │
60. Tool breaks (durability 0/60)
       │
       ├────────────────► damage_tool(1) returns true
       │                         │
       └────────────────► Remove tool from slot
```

## Class Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                            Item (enum)                          │
├─────────────────────────────────────────────────────────────────┤
│ • Block(BlockType)                                              │
│ • Tool(ToolType, ToolTier)                                      │
│ • Stick                                                         │
│ • Coal                                                          │
├─────────────────────────────────────────────────────────────────┤
│ + max_stack_size() -> u32                                       │
│ + can_stack_with(&Item) -> bool                                 │
│ + name() -> &str                                                │
└────────────────────┬────────────────────────────────────────────┘
                     │ used by
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                        ItemStack (struct)                       │
├─────────────────────────────────────────────────────────────────┤
│ • item: Item                                                    │
│ • count: u32                                                    │
│ • durability: Option<u32>                                       │
├─────────────────────────────────────────────────────────────────┤
│ + new(Item, u32) -> Self                                        │
│ + can_merge_with(&ItemStack) -> bool                            │
│ + merge(&mut ItemStack) -> u32                                  │
│ + split(u32) -> Option<ItemStack>                               │
│ + damage_tool(u32) -> bool                                      │
│ + durability_percent() -> Option<f32>                           │
└────────────┬────────────────────────────────┬──────────────────┘
             │ stored in                      │ stored in
             ▼                                ▼
┌──────────────────────────────┐  ┌──────────────────────────────┐
│   Inventory (struct)         │  │   CraftingGrid (struct)      │
├──────────────────────────────┤  ├──────────────────────────────┤
│ • slots: [Option<Stack>; 36] │  │ • size: usize (2 or 3)       │
│ • selected_slot: usize       │  │ • slots: Vec<Option<Stack>>  │
├──────────────────────────────┤  │ • output: Option<Stack>      │
│ + new() -> Self              │  ├──────────────────────────────┤
│ + selected_item() -> Option  │  │ + new_2x2() -> Self          │
│ + select_next()              │  │ + new_3x3() -> Self          │
│ + add_item(Stack) -> u32     │  │ + update_output()            │
│ + remove_item(Item, u32)     │  │ + take_output() -> Option    │
│ + swap_slots(usize, usize)   │  │ + set_slot(usize, Option)    │
│ + move_item(usize, usize)    │  └──────────────┬───────────────┘
└──────────────────────────────┘                 │ matches against
                                                 ▼
                                  ┌──────────────────────────────┐
                                  │      Recipe (struct)         │
                                  ├──────────────────────────────┤
                                  │ • pattern: RecipePattern     │
                                  │ • output: ItemStack          │
                                  ├──────────────────────────────┤
                                  │ + matches(&Grid) -> Option   │
                                  └──────────────┬───────────────┘
                                                 │ pattern type
                                                 ▼
                                  ┌──────────────────────────────┐
                                  │   RecipePattern (enum)       │
                                  ├──────────────────────────────┤
                                  │ • Shapeless(Vec<Item>)       │
                                  │ • Shaped2x2([Option<Item>])  │
                                  │ • Shaped3x3([Option<Item>])  │
                                  └──────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                           Tool (struct)                         │
├─────────────────────────────────────────────────────────────────┤
│ • tool_type: ToolType (Pickaxe/Axe/Shovel)                     │
│ • tier: ToolTier (Wood/Stone/Iron/Diamond)                     │
├─────────────────────────────────────────────────────────────────┤
│ + new(ToolType, ToolTier) -> Self                               │
│ + max_durability() -> u32                                       │
│ + mining_speed() -> f32                                         │
│ + is_correct_tool_for(BlockType) -> bool                        │
│ + can_harvest(BlockType) -> bool                                │
│ + break_time(BlockType) -> f32                                  │
└─────────────────────────────────────────────────────────────────┘
```

## State Transitions

### ItemStack State Machine

```
         ┌─────────────┐
         │   Created   │
         │  (count=n)  │
         └──────┬──────┘
                │
        ┌───────┴────────┐
        │                │
        ▼                ▼
  ┌──────────┐     ┌──────────┐
  │ Merged   │     │  Split   │
  │(count++)│     │(count--)│
  └────┬─────┘     └────┬─────┘
       │                │
       └────────┬───────┘
                │
         ┌──────▼──────┐
         │  In Slot    │
         │ (stored)    │
         └──────┬──────┘
                │
        ┌───────┴────────┐
        │                │
        ▼                ▼
  ┌──────────┐     ┌──────────┐
  │  Moved   │     │  Removed │
  │(swap)    │     │(count=0) │
  └──────────┘     └────┬─────┘
                        │
                        ▼
                   ┌─────────┐
                   │ Deleted │
                   └─────────┘

For Tools Only:
         ┌─────────────┐
         │  Full Dur.  │
         │  (dur=max)  │
         └──────┬──────┘
                │
                │ damage_tool(1)
                ▼
         ┌─────────────┐
         │  Damaged    │
         │ (dur < max) │
         └──────┬──────┘
                │
                │ (dur → 0)
                ▼
         ┌─────────────┐
         │   Broken    │
         │  (removed)  │
         └─────────────┘
```

### Crafting State Flow

```
┌─────────────┐
│ Empty Grid  │
└──────┬──────┘
       │ player places items
       ▼
┌──────────────────┐
│  Items in Grid   │
│  update_output() │
└──────┬───────────┘
       │
       ├─ matches recipe → Output filled
       │
       └─ no match → Output empty
              │
              ▼
       ┌──────────────┐
       │ Output Ready │
       └──────┬───────┘
              │ player clicks output
              ▼
       ┌──────────────────┐
       │  take_output()   │
       │ • Consume inputs │
       │ • Return output  │
       └──────┬───────────┘
              │
              ▼
       ┌──────────────┐
       │ Grid Updated │
       │ (ingredients │
       │  removed)    │
       └──────────────┘
```

## Memory Layout

### Inventory Structure (576 bytes)

```
Inventory {
    slots: [Option<ItemStack>; 36]    // 36 × 16 bytes = 576 bytes
           ↓
    ┌──────────────────────────────────────┐
    │ Hotbar (9 slots)                     │
    ├──────────────────────────────────────┤
    │ 0 │ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │ 7 │ 8 │
    ├───┴───┴───┴───┴───┴───┴───┴───┴───┤
    │                                      │
    │ Storage (27 slots)                   │
    │                                      │
    │  9 │10 │11 │   ...   │33 │34 │35 │
    └──────────────────────────────────────┘

    selected_slot: usize                   // 8 bytes
}

Each Option<ItemStack>:
    tag:        1 byte  (Some/None)
    padding:    3 bytes
    data:      12 bytes (ItemStack if Some)
    ────────────────────
    total:     16 bytes
```

### ItemStack Structure (12 bytes)

```
ItemStack {
    item:       Item            // 8 bytes (enum with variants)
    count:      u32             // 4 bytes
    durability: Option<u32>     // 8 bytes (1 tag + 4 value + 3 padding)
}                                  ──────
                                   20 bytes (actual)
                                   → Compiler packs to ~12-16 bytes
```

## Recipe Matching Algorithm

```
Input: CraftingGrid
Output: Option<ItemStack>

┌─────────────────────────────────────┐
│ For each recipe in RECIPES array:  │
└────────────┬────────────────────────┘
             │
             ▼
      ┌──────────────┐
      │ Pattern Type?│
      └──────┬───────┘
             │
     ┌───────┴────────┬──────────┐
     │                │          │
     ▼                ▼          ▼
┌──────────┐  ┌────────────┐  ┌────────────┐
│Shapeless │  │ Shaped 2x2 │  │ Shaped 3x3 │
└────┬─────┘  └─────┬──────┘  └─────┬──────┘
     │              │               │
     ▼              ▼               ▼
 Extract items   Check exact    Check exact
 from grid       positions       positions
     │              │               │
     ▼              ▼               ▼
 Sort both      Compare each    Compare each
 lists          slot with       slot with
                pattern         pattern
     │              │               │
     └──────┬───────┴───────────────┘
            │
            ▼
       ┌─────────┐
       │ Match?  │
       └────┬────┘
            │
      ┌─────┴─────┐
      │           │
      ▼           ▼
   ┌─────┐    ┌──────┐
   │ Yes │    │  No  │
   └──┬──┘    └───┬──┘
      │           │
      │           │ (try next recipe)
      │           │
      ▼           ▼
  Return        Continue
  output        loop
  ItemStack
```

## Performance Characteristics

```
Operation                Time         Space      Notes
─────────────────────────────────────────────────────────────────
Add item to inventory    O(36)        O(1)       Linear scan for merge/empty
Remove item              O(36)        O(1)       Scan all slots
Count item               O(36)        O(1)       Scan all slots
Select hotbar slot       O(1)         O(1)       Direct array access
Swap slots               O(1)         O(1)       Array swap
Recipe matching          O(13)        O(1)       13 recipes max
Crafting output          O(1)         O(1)       Cached result
Tool break time calc     O(1)         O(1)       Simple arithmetic
Stack merge              O(1)         O(1)       Min/max operations
─────────────────────────────────────────────────────────────────
```

## Integration Flow

```
┌──────────────┐
│  Main Game   │
│     Loop     │
└──────┬───────┘
       │
       ├─────────────► Input Handler
       │                    │
       │                    ├──► Number keys → select_slot()
       │                    ├──► Scroll wheel → select_next()
       │                    └──► Click → interact with blocks
       │
       ├─────────────► Physics System
       │                    │
       │                    ├──► Raycast → find target block
       │                    ├──► Break block → damage_tool()
       │                    └──► Place block → consume item
       │
       ├─────────────► Inventory System ◄─── (THIS SYSTEM)
       │                    │
       │                    ├──► Manage items
       │                    ├──► Track durability
       │                    └──► Match recipes
       │
       ├─────────────► UI Renderer
       │                    │
       │                    ├──► Render hotbar
       │                    ├──► Render inventory screen
       │                    └──► Render crafting UI
       │
       └─────────────► World Manager
                            │
                            ├──► Set/Get blocks
                            └──► Drop items
```

This architecture provides clean separation of concerns, minimal coupling, and efficient data flows for a Minecraft-style inventory system.
